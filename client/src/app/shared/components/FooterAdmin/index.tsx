import { cn } from "@/lib/utils";
import React from "react";

type Props = {};

const FooterAdmin = (props: Props) => {
  return (
    <footer className={cn("admin-footer", "fixed bottom-0 w-full py-8")}>
      FooterAdmin
    </footer>
  );
};

export default FooterAdmin;
