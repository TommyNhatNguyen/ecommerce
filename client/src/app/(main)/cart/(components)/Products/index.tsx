import React from "react";

type Props = {};

const Products = (props: Props) => {
  // Get products from cart
  // Get hooks that allow to update cart quantity, remove product from cart, change product variant
  // Display products
  //   Select all checkbox
  // Select checkbox
  // Image carousel
  // Product name
  // Variant name
  // Short description
  // Variant options
  // Product discount type
  // Price
  // Quantity with + - button
  // Subtotal
  // Total discount
  // Total
  // Remove button
  return <div>Products</div>;
};

export default Products;
