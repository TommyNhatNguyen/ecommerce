import React from "react";

type Props = {};

const Billing = (props: Props) => {
  return (
    <section id="billing" className="billing pt-section">
      <h2 className="text-2xl font-bold">Billing</h2>
    </section>
  );
};

export default Billing;
