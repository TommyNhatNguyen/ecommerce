import React from 'react'

type Props = {}

const UserPage = (props: Props) => {
  return (
    <div>
      Current user + role
      Add new user
      Update role of user
      Delete user
    </div>
  );
}

export default UserPage